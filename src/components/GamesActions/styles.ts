import { StyleSheet } from "react-native";

export const gameActionsStyle = StyleSheet.create({
  buttonsContent: {
    flexDirection: "row",
    alignItems: "center",
    width: "100%",
    paddingTop: 20,
    paddingBottom: 40,
    justifyContent: "space-between",
  },
});
