import { StyleSheet, Text, View } from "react-native";
import Base from "../../../components/Base/Base";

const Home: React.FC = () => {
  return (
    <Base>
      <Text>Home page</Text>
    </Base>
  );
};

export default Home;
