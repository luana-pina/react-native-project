import { StyleSheet, Text, View } from "react-native";
import Layout from "../../../components/Layout/Layout";

const Bets: React.FC = () => {
  return (
    <Layout>
      <Text>Bets page</Text>
    </Layout>
  );
};

export default Bets;
