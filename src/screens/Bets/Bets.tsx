import { StyleSheet, Text, View } from "react-native";
import Base from "../../../components/Base/Base";

const Bets: React.FC = () => {
  return (
    <Base>
      <Text>Bets page</Text>
    </Base>
  );
};

export default Bets;
